FROM graphistry/baseapp:1.2

# fetch secrets from our vault, do the npm installs, and delete the secrets.

# DEV-1144 # ARG BUILD_NUMBER
ENV APPDIR /app
WORKDIR $APPDIR

COPY ./wholly-innocuous/files/npm/rc /root/.npmrc

COPY ./central .

RUN npm install
RUN npm run build
RUN npm publish
