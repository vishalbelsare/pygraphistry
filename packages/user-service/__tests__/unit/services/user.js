describe('services/user.js', () => {
  
  describe('createUser', () => {
    it('does something', () => {
      expect(1).toBe(1);
    });
  });

  describe('getUserById', () => {
    it('does something', () => {
      expect(1).toBe(1);
    });
  });

  describe('updateUser', () => {
    it('does something', () => {
      expect(1).toBe(1);
    });
  });
})