describe('services/session', () => {
    describe('login', () => {
        it('does something', () => {
            expect(1).toBe(1);
        });
    });

    describe('logout', () => {
        it('does something', () => {
            expect(1).toBe(1);
        });
    });
});
